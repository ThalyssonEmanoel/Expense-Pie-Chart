git add . && git commit -m":sparkles: feat: Criado o cabeçalho, falta fazer listar tudo que foi cadastrado" && git push origin

PORT=3002 npm run dev

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash  && export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" && nvm use 21.7.3

pip install -r requirements.txt ---> Comando para executar a instalação dos requirements
,